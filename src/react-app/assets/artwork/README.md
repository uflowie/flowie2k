# Local artwork

Drop artwork files into this directory and restart the development server. The
artwork picker discovers them automatically; the filename without its extension
becomes the displayed name.

Supported formats: AVIF, GIF, JPEG, PNG, SVG, and WebP.

The image files in this directory are intentionally ignored by Git. This README
is the only tracked file.
